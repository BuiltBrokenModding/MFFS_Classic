# Access Transformer for MFFS - 1.12.2 (SRG names)
# ============================================================
# Format: <access> <class> <member>[(descriptor)]
# No space between method name and descriptor.
# Use SRG names for obfuscated members.
# ============================================================

# GuiContainer.drawSlot (private -> protected)
# MCP: drawSlot(Slot) -> SRG: func_146977_a
# Real signature in 1.12.2: private void drawSlot(Slot slotIn)
protected net.minecraft.client.gui.inventory.GuiContainer func_146977_a(Lnet/minecraft/inventory/Slot;)V

# NOTE: The following are already accessible in 1.12.2 and do NOT need ATs:
#   GuiContainer.isPointInRegion - already protected
#   GuiButton.getHoverState - already protected
#   EntityPlayerMP.server - already public
